import {
    helpCommandData,
    trollCommandData,
    gamesCommandData,
    citgenCommandData,
    dateCommandData,
    addCommandData,
} from '../commands';

export const commands: ICommand[] = [
    helpCommandData,
    trollCommandData,
    gamesCommandData,
    citgenCommandData,
    dateCommandData,
    addCommandData,
];
